# Development dependencies for SEKOIA.IO Splunk App
# Install with: pip install -r requirements-dev.txt

# Unit tests
pytest>=7.0.0,<8.0.0
pytest-cov>=3.0.0,<4.0.0
# Integration tests
# Splunk app inspection
splunk-appinspect>=4.2.0,<5.0.0
